namespace Common.Enum
{
    public class EDatabase
    {
        public enum EContactColumnsName
        {
            All, Name, Email, Phone
        }

        public enum ESlidePosition
        {
            SlideShow, HomeBanner, HomeAside, PhoneTop
        }
    }
}
